import { API_URL } from 'src/common/constant/router';

export const InitRouter = {
  ROOT: `${API_URL}/init`,
  NEW_WORKSPACE: '/new-workspace',
  WORKSPACE_100TY_UPDATE: '/workspace-100ty-update',
};
